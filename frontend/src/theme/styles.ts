const styles = {
  global: () => ({
    html: { height: '100%' },
    body: { margin: 0, backgroundColor: 'white' },
    '#root': { height: '100vh' },
    a: { outline: 'none' },
    button: { outline: 'none' },
  }),
};

export default styles;
